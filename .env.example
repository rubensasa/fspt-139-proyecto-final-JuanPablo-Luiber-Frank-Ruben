# This file includes global variables that will be available inside your project
# 1. In the front end code you can access this variables like this: console.log(import.meta.env.VARIABLE_NAME)
# 1. In the back end code you access the variable by importing os and then typing print(os.getenv('VARIABLE_NAME'))

# Back-End Variables
DATABASE_URL=postgres://gitpod:postgres@localhost:5432/example
FLASK_APP_KEY="any key works"
FLASK_APP=src/app.py
FLASK_DEBUG=1
DEBUG=TRUE

# Clave para firmar la sesión de Flask (usada por /api/steam/login para el flujo OpenID)
SECRET_KEY="cambia-esto-por-una-clave-secreta"
# Clave para firmar los JWT de sesión
JWT_SECRET_KEY="cambia-esto-por-otra-clave-secreta"

# URL a la que Steam redirige tras el login OpenID (debe apuntar a /api/steam/callback)
STEAM_RETURN_URL=http://localhost:3001/api/steam/callback
# URL del frontend, usada por CORS y para redirigir tras vincular Steam
VITE_FRONTEND_URL=http://localhost:3000

# API key de steamapis.com (perfil de Steam en /api/steam/profile) — https://steamapis.com
API_KEY=

# API key oficial de Steam (juegos y logros reales) — gratuita, se obtiene en
# https://steamcommunity.com/dev/apikey
STEAM_API_KEY=

# Front-End Variables
VITE_BASENAME=/
#VITE_BACKEND_URL=
